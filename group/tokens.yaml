
active_security_tokens:
  name: Active Security Tokens
  entities:
    - device_tracker.token_jared
    - device_tracker.token_chelsea
    - device_tracker.token_flaps
    - device_tracker.token_bella
    - device_tracker.token_purple
    - device_tracker.token_nick

security_tokens:
  name: Security Tokens
  control: hidden
  entities:
    - device_tracker.token_jared
    - device_tracker.token_chelsea
    - device_tracker.token_flaps
    - device_tracker.token_bella
    - device_tracker.token_purple
    - device_tracker.token_nick
    - device_tracker.token_guest1
    - device_tracker.token_guest2
    - device_tracker.token_guest6

presence_jared:
  name: Jared
  entities:
    - device_tracker.google_maps_115620344348341532807
    - device_tracker.token_jared
    - device_tracker.10101029_cc2d83126115


